name: P3.5b Energy-Aware Life Stage Patch

on:
  push:
    branches:
      - feature/p35-energy-aware-prey-selection

permissions:
  contents: write

concurrency:
  group: p35b-energy-aware-life-stage-patch
  cancel-in-progress: true

jobs:
  patch-and-validate:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Apply P3.5b patch
        run: node scripts/apply-p35b-energy-aware-life-stage.mjs
      - name: Typecheck
        run: npm run lint
      - name: Run 90-day hunt diagnostic
        run: npx tsx scripts/predator-hunt-event-probe.ts --out=artifacts/p35b-life-stage
      - name: Commit core change
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/simulation/predatorDiscreteHuntingSystem.ts
          git commit -m "tune: make life-stage selection energy aware"
          git push origin HEAD:feature/p35-energy-aware-prey-selection
